{
  "extends": "@react-native/typescript-config/tsconfig.json",
  // 이 아래로가 추가한것
  "compilerOptions": {
    // "baseUrl": "src",
    // "paths": {
    //   "~/*": ["./*"]
    // },
    "jsx": "react-jsx",
    "allowJs": true,
    "declaration": true,
    "types": ["@types/styled-components-react-native"],
    "moduleResolution": "node", // TBD | typescript 5.4(beta) 에서는 이거 없이도 타입 적용된다는 글도 있어서 나중에 확인
  },
  // "include": [
  //     "src"
  // ]
}